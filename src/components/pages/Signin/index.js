import React from 'react';
import Container from 'components/templates/Container';

function Signin({ history }) {
  return (
    <Container history={history} noHeader>
      <h1>Signin</h1>
    </Container>
  );
}

export default Signin;
